# Configuración de build para Netlify.
# El frontend vive en el subdirectorio "frontend" del repositorio.
[build]
  base = "frontend"
  command = "npm run build"
  publish = ".next"

# Plugin oficial de Next.js (Netlify lo instala automáticamente).
[[plugins]]
  package = "@netlify/plugin-nextjs"
